import React from 'react'

// const Button = ({ onClick, className, children, ...restProps }) => (
//   <button onClick={onClick} className={`button ${className}`} {...restProps}>
//     {children}
//   </button>
// )

// export default Button