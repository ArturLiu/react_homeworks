// import React from 'react'

// const Input = ({ placeholder, className, ...restProps }) => (
//   <input type="text" placeholder={placeholder} className={`input ${className}`} {...restProps} />
// )

// export default Input
